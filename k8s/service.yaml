apiVersion: v1
kind: Service
metadata:
  name: payroll-api
  labels:
    app.kubernetes.io/name: payroll-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: payroll-api
  ports:
    - name: http
      port: 80
      targetPort: http
